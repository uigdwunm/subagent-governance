# 项目 Agent 规范

## 项目定位

- 本项目用于规范化管理 Codex 原生子 Agent，使子 Agent 的任务派发、消息传递、上下文交接、执行等待、异常恢复、生命周期管理和终态验收更加清晰、可靠且可诊断。
- 本项目解决的是父 Agent 与子 Agent 之间的协作治理问题，不替代子 Agent 的业务执行能力，也不引入第二套编排平台。
- 治理规则应同时服务于用户可理解性和运行可靠性：用户能看懂为什么派发、如何配置和当前处于什么状态；Agent 能依据明确契约执行、等待、恢复和验收。

## 对外文案边界

- README、插件清单、Marketplace 描述、GitHub 仓库简介、发布说明和社交预览等对外文案，只可说明本项目相对 Codex 原生子 Agent 能力增加了什么。
- 对外文案不点名、不影射、不比较其他产品、插件、Skill 或编排组件，也不使用“市场唯一”“领先同类”等无法稳健证明的表述。
- 对比应保持中性：Codex 原生工具仍是唯一执行通道，本项目只增加任务契约、材料校验、精确身份、生命周期对账和诊断能力，不宣称替代或修复 Codex 平台内部能力。

## 开发与安装边界

### 默认对象与唯一开发源

- 本项目中的“项目”“插件”或“本插件”默认指开发仓库内容，不代表或授权操作本机已安装的同名插件；需要讨论已安装实例时必须明确指出目标位置和操作。
- 本目录是 `subagent-governance` 的开发仓库和唯一开发源。日常开发和其他位置反馈问题的修复只能修改本仓库，不得先修改本机插件或运行缓存再反向补开发仓库。
- 日常开发不得直接修改 `~/plugins/subagent-governance` 或 `~/.codex/plugins/cache/personal/subagent-governance`。

### 授权与隔离

- 开发验证本身不构成同步、安装或部署授权；如需同步或更新本机插件，必须先取得用户在当前任务中的明确授权。
- 只有用户在当前任务中明确授权相应操作时，才允许写入本机插件目录、稳定发布源、Marketplace、运行缓存、Hook 信任状态或 Codex Manager Registry。
- 开发仓库、稳定发布源和运行缓存之间不得创建符号链接。
- 安装布局可以为尚未重启的既有任务保留精确一个不可变的上一版本缓存；该文件存活策略不允许当前插件读取旧状态、加载旧逻辑或增加旧版本兼容分支。
- 不修改第三方 Skill；相关集成边界只记录在本项目文档中。

## 外部问题修复、部署与真实测试

### 开发仓库阶段

- 本项目是插件项目，问题可能由其他项目、对话或插件实际运行环境反馈回来；处理这类问题时，必须先在本项目中复现、修复并完成相应本地验证。
- 只有取得用户在当前任务中的明确授权后，才能使用本项目的最新内容更新用于测试的本机插件。

### 部署事务

- 获准更新本机插件后，部署命令必须在同一事务内完成写入、失败回滚和最终校验。
- 无论部署命令成功或失败，命令结束后当前任务都只能报告结果并立即停止，等待用户重启 Codex，不得继续执行其他命令或用当前进程验证新版。
- 部署失败且未能安全回滚时，必须如实报告恢复状态，并在用户重启后的新任务中处理。

### 重启后真实测试与验收

- 用户确认重启后，真实插件测试必须在开发任务之外的新任务中进行，不复用实现或调试该问题的原任务。
- 创建、交接或指导用户创建真实测试任务时，必须显式指定模型 `gpt-5.6-terra`、推理强度 `high`，不得依赖项目外任务继承本文件；只有用户明确指定其他配置时才可覆盖。无法确认真实测试任务的实际配置时，必须将该项标记为未验证。
- 只有用户已授权部署和真实测试时，完整验收才同时包含开发仓库内的验证结果和重启后的新任务验证；任一获授权环节未执行或未通过时，不得宣称问题已完整修复。
- 未获授权时只能报告开发仓库内的验证结果，并明确说明尚未部署或真实测试。

## 核心治理目标

### 派发透明且任务明确

- 默认派发子 Agent 时，必须让用户知道为什么发起、要解决什么问题、允许处理什么范围以及怎样才算完成。
- 模型、推理强度、上下文继承方式、治理等级等关键派发参数应按统一格式向用户说明；自然语言说明使用中文明文，模型名、参数值、命令、路径和 Agent 标识等技术内容可以保留原文。
- 子 Agent 收到的任务必须有唯一当前目标和可验证的完成条件，避免把旧任务、旧确认或父线程历史误当成更高优先级指令。

### 执行期间有序等待和有限干预

- 子 Agent 正常执行期间，父 Agent 不应通过反复查看代码、日志、测试或进度来猜测状态，也不应在没有新证据时随意指挥或打断子 Agent。
- 父 Agent 应以终态通知为主要完成信号，并使用规范的等待、超时巡检和状态对账流程识别正常长耗时、静默停止、平台错误和真实阻塞。
- 发生可恢复异常时，优先恢复同一个子 Agent 和原任务，保留已有进度；重试必须有明确上限，不能形成无限追问、重复派发或假运行状态。

### 上下文交接充分且克制

- 每次派发都应显式选择隔离、有限继承或完整继承，并使上下文规模与任务实际需要相匹配。
- 子 Agent 必须获得足以独立执行的目标、范围、禁止事项、完成条件和必要背景，不能依赖隐式历史或自行猜测父任务意图。
- 不应为了“保险”无差别继承全部历史；无关上下文会稀释当前目标、引入旧指令并增加任务漂移风险。
- 父 Agent 应保留足够的任务摘要、Agent 映射和恢复信息，使上下文压缩或会话恢复后仍能继续等待、通信和验收，而不是重复创建子 Agent。

### 终态可验收且责任闭环

- 子 Agent 完成、阻塞或需要决策时，必须向父 Agent 发送明确终态通知；仅停止运行、完成代码或创建提交，不等于生命周期已经闭环。
- 终态应根据治理等级提供实际结果、验证证据、剩余事项和父任务下一步，避免只有“已完成”“收到”或缺乏证据的状态声明。
- 父 Agent 应区分执行完成、证据不足、平台错误、协议不匹配、真实阻塞和需要用户决策，避免错误验收或错误重试。

### 治理异常不阻断原生通道

- 本项目始终使用原生 `spawn_agent` 作为派发通道。治理层自身发生异常时，应按明确的 fail-open 边界放行，不能因为 Hook、状态文件或协议异常阻断原生 `spawn_agent`；同时应保留可定位的诊断信息。
- 对消息不可见、平台断流、状态损坏或 provider 协议不匹配等边界，应如实描述能够检测和缓解的范围，不宣称修复 Codex 平台内部能力。

## 实现原则

- 保留原生 `spawn_agent`，不引入第二套编排平台。
- Hook 只做用户级协作护栏，不宣称替代沙箱、批准机制或平台内部消息投递。
- 优先修复会导致错误阻断、错误终态或无法恢复的状态机问题。
- 对 unmanaged 原生调用保持明确的透传边界，避免治理组件故障时禁用原生子 Agent。
- 协议、Skill、Hook 和 Schema 应尽量共享同一语义来源，避免重复规则漂移。

## 验证要求

### 运行时代码与 Skill

- 修改运行时代码时，先补能稳定复现问题的测试，并至少运行：
  - `python3 -m unittest discover -s tests -v`
  - `python3 -m compileall -q scripts`
  - `python3 ~/.codex/skills/.system/plugin-creator/scripts/validate_plugin.py .`
  - `python3 ~/.codex/skills/.system/skill-creator/scripts/quick_validate.py skills/subagent-governance`（修改 Skill 时）

### 文档

- 仅修改文档或 `AGENTS.md` 时，至少运行 `git diff --check` 以及与修改内容直接相关的结构检查或验证器；除非文档参与运行时契约或测试断言，否则不强制运行全量运行时测试。

### 发布与部署

- 发布前必须验证开发仓库与稳定发布源不是同一路径或符号链接。
- 只有获准部署本机插件时，部署命令才必须在事务结束前验证稳定发布源与目标运行缓存哈希一致；未获部署授权时，必须将运行缓存一致性标记为未验证，不得为完成发布前检查而安装或修改本机插件。
